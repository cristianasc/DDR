function r = BuildNeighbour(CurrentSolution, lambda, i)
    Matrizes;
    miu= R*1e9/(8*1000);
    T(:,3:4)= T(:,3:4)*1e6/(8*1000);
    gama= sum(sum(T(:,3:4)));
    d= L*1e3/2e8;
    nT= size(T,1);
    
    
end
